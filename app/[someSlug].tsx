import {SafeAreaView, Text} from 'react-native';

// Dynamic match with route like /expo or /someText

export default function Page() {
    return (
        <SafeAreaView>
            <Text>Some page with slug in file name</Text>
        </SafeAreaView>
    );
}
