import {SafeAreaView, Text} from 'react-native';

// Match with /settings route

export default function Settings() {
    return (
        <SafeAreaView>
            <Text>Settings page in settings folder</Text>
        </SafeAreaView>
    );
}
