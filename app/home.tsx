import {SafeAreaView, Text} from 'react-native';

// Math with /home path in route

export default function Home() {
    return (
        <SafeAreaView>
            <Text>Home peage</Text>
        </SafeAreaView>
    );
}
